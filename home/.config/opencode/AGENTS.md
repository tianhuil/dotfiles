You have access to some skills:

- `coding-standards`: Follow coding standards and best practices
- `merge-conflict`: Resolve git merge conflicts safely
- `agent-browser`: vercel labs agent browser, replacement for playwright

## Native Tools (replaced MCPs)

- `sequential_thinking`: Structured chain-of-thought reasoning is provided natively by the model provider — no MCP needed.
- `web_search_prime`: Web search is available via OpenCode's built-in `websearch` tool powered by Exa AI (enable with `OPENCODE_ENABLE_EXA=1`).
