# npm user config is redirected to the gitignored ~/.npmrc.secrets via
# NPM_CONFIG_USERCONFIG in home/shell/.coreenv, so `npm login` never writes
# auth tokens into this tracked (stowed) file. Settings below are non-secret
# and safe to commit.
